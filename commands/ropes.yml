name: ropes
description: Returns all ropes attached to a ropes owner transport vehicle.
groups:
- Ropes and Sling Loading
syntax:
- call: !Unary ropesOwner
  ret:
  - Unknown
  - array of the actual rope objects
  params:
  - name: ropesOwner
    description: a transport from which the ropes originate
    type: Unknown
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma_3:
    major: 1
    minor: 34
examples:
- hint str (ropes vehicle player);
- _rope1 = (ropes heli1) select 0;
