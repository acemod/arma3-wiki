name: hiddenActions
description: Getter for [[hideActions]], see also [[shownAction]].
groups:
- Interaction
syntax:
- call: Nular
  ret:
  - ArrayUnknown
  - Array of [[Number]]s or []. See [[shownAction]] for numbers meaning.
  params: []
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma_3:
    major: 2
    minor: 22
examples:
- <sqf>if (count hiddenActions == 0) then { hint "No actions have been harmed during script execution" };</sqf>
