name: inAreaArrayIndexes
description: |-
  Returns indices of positions that are within the provided area.

  {{Feature|important|Unlike [[inAreaArray]], ''area'' position is [[Position#PositionASL|PositionASL]].}}
groups:
- Triggers
- Markers
- Locations
- Positions
- Groups
syntax:
- call: !Binary
  - positions
  - area
  ret:
  - Unknown
  - indices of ''positions'''s matching elements
  params:
  - name: positions
    type: Unknown
  - name: area
    type: Unknown
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma_3:
    major: 2
    minor: 14
examples:
- "<sqf>\nprivate _players = call BIS_fnc_listPlayers;\nprivate _indices = _players inAreaArrayIndexes _marker;\n{\n\tprivate _player = _players select _x;\n\t[\"You are in the marker!\"] remoteExec [\"hint\", _player];\n} forEach _indices;\n</sqf>"
