name: lnbSetPictureRight
description: |-
  Sets the cell's right-picture. The picture is set to the end of the '''row''', the ''column'' parameter does not matter.
  {{Feature|informative|The picture is searched in the mission, directory, the campaign directory's dtaExt subdirectory, and the data bank (or directory).}}
groups:
- GUI Control - ListNBox
syntax:
- call: !Unary
  - idc
  - - row
    - column
  - path
  ret:
  - Nothing
  - null
  params:
  - name: idc
    type: Number
  - name: row
    description: 0-based index (first row is 0, second is 1, etc)
    type: Number
  - name: column
    description: 0-based index (first column is 0, second is 1, etc)
    type: Number
  - name: path
    description: the picture's file path
    type: String
- call: !Binary
  - ctrl
  - - - row
      - column
    - path
  ret:
  - Nothing
  - null
  params:
  - name: ctrl
    type: Control
  - name: row
    description: 0-based index (first row is 0, second is 1, etc)
    type: Number
  - name: column
    description: 0-based index (first column is 0, second is 1, etc)
    type: Number
  - name: path
    description: the picture's file path
    type: String
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma_3:
    major: 1
    minor: 0
examples:
- <sqf>lnbSetPictureRight [1, [0, 0], "Picture.paa"];</sqf>
- <sqf>_ctrl lnbSetPictureRight [[0, 0], "\A3\Ui_f\data\GUI\Cfg\Ranks\lieutenant_gs.paa"];</sqf>
