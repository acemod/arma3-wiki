name: modelToWorldVisual
desc: |-
  Converts position from object model space to world space in render time scope. For ASL version see [[modelToWorldVisualWorld]].
  {{Feature|important|For [[setObjectScale|scaled objects]], the relative position will first be [[vectorMultiply|multiplied]] by the object scale.<br>
  For example, if the object scale is 2, <sqf inline>_obj modelToWorldVisual [0,1,0]</sqf> will be offset '''2 meters''' from the model center (<sqf inline>[0,0,0]</sqf>).
  }}
groups:
- Object Manipulation
- Render Time Scope
syntax:
- call: !Binary
  - object
  - modelPos
  ret:
    type: Position3dAGL
  params:
  - name: object
    type: Object
  - name: modelPos
    desc: in format [[Position#PositionRelative|PositionRelative]] ([x,y,z] offset relative to the model centre)
    type: ArrayUnknown
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma3: '1.32'
examples:
- <sqf>_aboveAndBehindPlayer = player modelToWorldVisual [0,-1,3];</sqf>
see_also:
- modelToWorld
- vectorModelToWorldVisual
- getPosWorldVisual
- worldToModelVisual
- worldToModel
- selectionPosition
