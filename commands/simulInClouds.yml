name: simulInClouds
description: Returns if given position is in clouds.
problem_notes:
- '{{Feature|edeneditor|SimulWeather seems to be disabled in [[:Category:Eden Editor|Eden Editor]] and scenario preview.}}'
groups:
- Environment
syntax:
- call: !Unary pos
  ret:
  - Unknown
  - |-
    [[true]] if given position is in clouds
    * [[Nothing]] - If given arguments are incorrect
  params:
  - name: pos
    type: Position
argument_loc: Unspecified
effect_loc: Unspecified
since:
  arma_3:
    major: 1
    minor: 0
examples:
- <sqf>simulInClouds getPos player;</sqf>
